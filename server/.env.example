# Server Configuration
PORT=3001
NODE_ENV=production

# Email Recipients
TO_EMAIL=ginoludik@gmail.com
FROM_EMAIL=noreply@tpshop.be

# AWS SES Configuration (Recommended for AWS)
AWS_SES_USER=your-ses-smtp-username
AWS_SES_PASSWORD=your-ses-smtp-password

# Alternative: Gmail Configuration (for testing)
# EMAIL_USER=your-email@gmail.com
# EMAIL_APP_PASSWORD=your-app-specific-password

# Bot Configuration (if using)
BOT_TOKEN=your-bot-token

# Database Configuration (if using)
DB_PASSWORD=your-secure-password